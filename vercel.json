{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @sly/api vercel-build",
  "experimentalServices": {
    "api": {
      "entrypoint": "apps/api",
      "routePrefix": "/"
    }
  }
}
